# Archived Node/Fastify backend

This directory is the previous backend (Fastify + Prisma + MinIO).
The active API is Laravel in [`../api`](../api).

Kept for reference only; not used for shared-hosting deploy.
